//Factorial
//codechef.com
#include <bits/stdc++.h>
using namespace std;
#define ll long long int
#define FAST ios_base::sync_with_stdio(false);cin.tie(NULL);
int32_t main() {
    ll t;
    FAST;
    cin>>t;
    while(t--)
    {
    	
    	int n;
    	cin>>n;
    	int ans=0;
    	for(int i=5;n/i>=1;i*=5)
    	{
    		ans+=n/i;
    	}
    	cout<<ans<<endl;
    }
    return 0;
}
